#!/bin/bash
# Usa el directorio de neovim si está disponible, si no fallback a $PWD
TARGET_DIR="${TMUX_NVIM_CWD:-$PWD}"
cd "$TARGET_DIR" || exit 1
exec direnv exec "$PWD" /Users/jmurphy/.opencode/bin/opencode "$@"
